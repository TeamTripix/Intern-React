import React from 'react'
import DeleteProduct from './deleteProduct'

const Index = (props)=>{
    return(
        <>
        <DeleteProduct pId={props.pId}/>
        </>
    )
}

export default Index